int max(int a, int b) {
  if (a > b) {
    return a;
  }
  return b;
}

int min(int a, int b) {
  if (a < b) {
    return a;
  }
  return b;
}

int main() {
  int a = 2 + 2 * 2;
  int b = 120 / 9 - 8;
  print(a % b);
  print(max(a, b));
  print(min(a, b));

  return 0;
}
