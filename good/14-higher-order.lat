int f(int(int) modifier, int x) {
  return modifier(x);
}

int main() {
  auto incr = (int x) => int { return x + 1; };
  auto add2 = (int x) => int { return x + 2; };
  auto div2 = (int x) => int { return x / 2; };

  print(f(incr, 10));
  print(f(add2, -15));
  print(f(div2, 43));

  return 0;
}
